---
import { cn } from "@lib/utils";
import Container from "./Container.astro";
import { button } from "@tailus/themer-button";
import Frame from "./Frame.astro";
---

<section
  class="relative overflow-hidden scroll-mt-24 min-h-screen flex items-center"
  id="home"
>
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0 z-0">
    <img
      src="/hero_bg_professional.png"
      alt="Gym Background"
      class="w-full h-full object-cover"
    />
    <div
      class="absolute inset-0 bg-gradient-to-b from-white/90 via-white/50 to-white/90 dark:from-gray-950/90 dark:via-gray-950/60 dark:to-gray-950/90"
    >
    </div>
  </div>

  <Container class="relative z-10">
    <div class="relative pt-12 sm:pt-0 ml-auto">
      <div class="lg:w-2/3 text-center mx-auto">
        <h1
          class="text-gray-900 dark:text-white font-bold text-3xl sm:text-5xl md:text-6xl lg:text-7xl tracking-tight"
        >
          Definizione Muscolare <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-primary-600 to-secondary-500"
            >Estrema</span
          >
        </h1>

        <p
          class="max-w-xl mx-auto mt-6 text-base sm:text-lg text-gray-700 dark:text-gray-300 leading-relaxed"
        >
          Il percorso scientifico per trasformare il tuo fisico natural al
          livello successivo, senza improvvisazione e senza perdere massa.
        </p>

        <p
          class="max-w-xl mx-auto mt-4 text-sm sm:text-base text-gray-600 dark:text-gray-400"
        >
          Sono Gabriele, coach e personal trainer specializzato in definizione e
          bodybuilding natural. Lavoro con chi vuole risultati veri, misurabili
          e costruiti su progressioni logiche.
        </p>

        <div class="mt-8 flex flex-wrap justify-center gap-4">
          <Frame>
            <a
              href="#contatti"
              class={cn(
                button.primary.lg,
                "relative z-[1] shadow-lg shadow-primary-500/20 w-full sm:w-auto",
              )}
            >
              Richiedi la tua consulenza
            </a>
          </Frame>

          <Frame>
            <a
              href="#metodo"
              class={cn(button.secondary.lg, "relative z-[1] w-full sm:w-auto")}
            >
              Scopri il metodo
            </a>
          </Frame>
        </div>
      </div>
    </div>
  </Container>
</section>
